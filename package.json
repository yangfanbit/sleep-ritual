{
  "name": "sleep-ritual",
  "version": "1.5.0",
  "private": true,
  "description": "跨平台睡前行为干预 PWA — 无头测试套件与 CI（jsdom + fake-indexeddb，无构建步骤）",
  "scripts": {
    "test": "node tools/run-tests.mjs all",
    "test:unit": "node tools/run-tests.mjs unit",
    "test:smoke": "node tools/run-tests.mjs smoke"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
